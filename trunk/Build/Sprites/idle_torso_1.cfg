
// Number of frames in the animation
frames = 1;

// Index of the frame to start at
startframe = 1;

// Filenames prefix of the animation
prefix = "idle_torso_1";

// Delay in milliseconds per frame
frametime = 50;

// Repeat animation?
repeat = true;
