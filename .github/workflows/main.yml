name: Main
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '0 0 * * 6'

  workflow_dispatch:

jobs:
  main:
    name: 'main.windows'
    runs-on: 'windows-2022'

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup VS Dev Environment
        uses: seanmiddleditch/gha-setup-vsdevenv@v4

      - name: Build
        run: devenv.exe Source\Launcher.sln /Build
